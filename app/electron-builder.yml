appId: com.hansenhomeai.whisper-maxxing
productName: Whisper Maxxing
asar: true
afterPack: build/afterPack.mjs
directories:
  output: release
files:
  - dist-electron/**
  - dist-renderer/**
  - package.json
extraResources:
  - from: bin/wdctl.mjs
    to: bin/wdctl.mjs
artifactName: ${productName}-${version}-${os}-${arch}.${ext}
mac:
  extraResources:
    - from: native/macos-capture/.build/release/whisper-mac-capture
      to: bin/whisper-mac-capture
  category: public.app-category.productivity
  hardenedRuntime: true
  gatekeeperAssess: false
  icon: build/icon.icns
  entitlements: build/entitlements.mac.plist
  entitlementsInherit: build/entitlements.mac.plist
  extendInfo:
    NSMicrophoneUsageDescription: Whisper Maxxing uses the microphone only while capturing local dictation.
  target:
    - dmg
    - zip
dmg:
  sign: false
  writeUpdateInfo: false
win:
  icon: build/icon.ico
  target: nsis
nsis:
  oneClick: false
  allowToChangeInstallationDirectory: true
  perMachine: false
  createDesktopShortcut: true
  createStartMenuShortcut: true
  shortcutName: Whisper Maxxing
